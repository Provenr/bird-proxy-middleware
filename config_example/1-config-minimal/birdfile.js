var birdConfig = {
    
    // 最基础的设置方案, 只需设置 静态文件 的根文件夹即可
    root: '../static-files',

    // 也可以用绝对路径来定义这个静态文件夹
    // root: require('path').resolve(__dirname + '/../static-files'),
}

module.exports = birdConfig